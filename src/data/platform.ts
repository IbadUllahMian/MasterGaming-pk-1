// Public competitive data must be supplied by registered users and verified results. No sample records ship to production.
export type TournamentStatus = 'REGISTERING' | 'ROOM_READY' | 'LIVE' | 'UPCOMING' | 'REGISTRATION_OPEN' | 'REGISTRATION_CLOSED' | 'FULL' | 'COMPLETED';
export type GameId='free-fire'|'pubg-mobile'|'call-of-duty-mobile'|'valorant'; export type TournamentMode='br'|'cs'|'lone-wolf';
export type Tournament = { id:string; name:string; shortDescription:string; game:GameId; mode:TournamentMode; status:TournamentStatus; date:string; startTime:string; endTime:string; registrationDeadline:string; totalSlots:number; registeredSlots:number; entryType:'Free'|'Paid'; prizeInfo:string; banner:string; rules:Record<string,string[]>; format:string; matchSchedule:{match:string;date:string;time:string;status:string;map:string}[]; organizer:string; region:string; createdDate:string; startsAt:string; currentMatch?:string; currentRound?:string; nextMatch?:string; entryFee?:string; prizePool?:string; brConfig?:{participation:'Solo'|'Duo'|'Squad';maxPlayers:number;matchCount:number;map:string;placementScoring:string;killScoring:string}; csConfig?:{teamFormat:'4v4';bestOf:'BO3'|'BO5';bracket:'Single elimination'}; loneWolfConfig?:{participation:'1v1'|'2v2';bracket:'Single elimination'}; roomAccess?:{unlockMinutesBefore:number;roomId?:string;password?:string;instructions?:string} };
export const tournaments: Tournament[] = [];
export const navItems = [['Home','/'],['Tournaments','/tournaments'],['Teams','/teams'],['Players','/players'],['Leaderboard','/leaderboard'],['Credits','/credits'],['Membership','/membership'],['News','/news'],['About','/about']] as const;
export const leaders: string[][] = [];
export const news = [['Platform','How verification works before standings are published',''],['Community','Preparing your player profile for tournament entry','']];
export const adminPanelContent = { title:'Tournament desk', copy:'Operational views become populated only through verified platform data. The platform CMS continues to own the /admin route.' };
