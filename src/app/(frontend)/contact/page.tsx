import { InfoPage } from '@/components/platform/Platform'; import { pageMetadata } from '@/data/seo';
export const metadata=pageMetadata('Contact the Tournament Desk','Contact the MasterGaming.pk tournament desk for Free Fire event questions, entry support, and tournament information.','/contact');
export default function Page(){return <InfoPage title="Talk to the tournament desk." copy="For event questions, use the existing manual entry and contact paths. A dedicated player support workflow is planned for a later phase."/>}
