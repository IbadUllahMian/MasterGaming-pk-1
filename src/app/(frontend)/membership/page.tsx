import { MembershipPage } from '@/components/finance/FinanceExperience'; import { privateMetadata } from '@/data/seo';
export const metadata=privateMetadata('Manual Membership Review','Request a MasterGaming.pk membership review through the platform’s manual process.','/membership'); export default function Page(){return <MembershipPage/>}
