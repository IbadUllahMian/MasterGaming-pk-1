import { ReferralPage } from '@/components/finance/FinanceExperience'; import { privateMetadata } from '@/data/seo';
export const metadata=privateMetadata('Manual Referral Reward Credits','View verified MasterGaming.pk referral records and manually reviewed reward credits.','/referrals'); export default function Page(){return <ReferralPage/>}
