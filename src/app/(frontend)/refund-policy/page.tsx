import { LegalPage } from '@/components/support/TrustExperience'; import { trustPages } from '@/data/trust'; import { pageMetadata } from '@/data/seo'; export const metadata=pageMetadata('Manual Refund Review Policy','Understand how MasterGaming.pk handles tournament entry refund reviews through a manual desk process.','/refund-policy'); export default function Page(){return <LegalPage page={trustPages['refund-policy']}/>}
