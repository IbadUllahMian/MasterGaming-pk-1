import { TournamentDetails } from '@/components/tournaments/TournamentExperience'; import { pageMetadata } from '@/data/seo'; export const metadata=pageMetadata('Tournament details and registration','View tournament rules, schedule, slots, prizes, and register through your MasterGaming player account.','/tournaments'); export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;return <TournamentDetails id={id}/>}
