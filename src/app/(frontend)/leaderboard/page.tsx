import { LeaderboardPage } from '@/components/platform/Platform'; import { pageMetadata } from '@/data/seo';
export const metadata=pageMetadata('Free Fire Player Leaderboard','Review verified MasterGaming.pk Free Fire player standings by mode when completed results are available.','/leaderboard'); export default function Page(){return <LeaderboardPage/>}
