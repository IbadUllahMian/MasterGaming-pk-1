import { LegalPage } from '@/components/support/TrustExperience'; import { trustPages } from '@/data/trust'; import { pageMetadata } from '@/data/seo'; export const metadata=pageMetadata('Manual Withdrawal Request Rules','Read MasterGaming.pk rules for manual withdrawal requests, review states, and payout-detail updates.','/withdrawal-rules'); export default function Page(){return <LegalPage page={trustPages['withdrawal-rules']}/>}
