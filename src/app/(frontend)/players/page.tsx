import { Players } from '@/components/accounts/AccountExperience'; import { pageMetadata } from '@/data/seo';
export const metadata=pageMetadata('Free Fire Player Directory','Explore verified MasterGaming.pk player profiles, regions, modes, and tournament performance.','/players'); export default function Page(){return <Players/>}
