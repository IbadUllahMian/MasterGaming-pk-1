import type { RankingsTableBlock as RankingsTableBlockType } from '@/payload-types';

export function RankingsTable({ eyebrow, heading, body, teams }: RankingsTableBlockType) {
  const playerBody = body === 'Current leaderboard presentation. Match points and placement are tracked per registered squad.' ? 'Current leaderboard presentation. Match points and placement are tracked per registered player.' : body;
  return <section className="bg-black px-5 py-20 sm:px-8 md:py-32"><div className="mx-auto grid max-w-7xl gap-10 lg:grid-cols-[.75fr_1.25fr] lg:items-end"><div className="reveal"><p className="kicker">{eyebrow ?? 'Live standings'}</p><h2 className="mt-5 display-title text-4xl sm:text-6xl">{heading}</h2>{playerBody ? <p className="body-copy mt-5">{playerBody}</p> : null}</div><div className="dark-card overflow-hidden reveal"><div className="grid grid-cols-[44px_1fr_58px_68px] border-b border-white/8 px-5 py-4 text-[10px] font-semibold uppercase tracking-[.16em] text-white/40"><span>#</span><span>Player</span><span className="text-right">M</span><span className="text-right">PTS</span></div>{(teams ?? []).map((team) => <div key={team.id} className="grid grid-cols-[44px_1fr_58px_68px] items-center border-b border-white/8 px-5 py-4 last:border-0"><span className="font-mono text-sm text-[#d3ff24]">{team.rank}</span><span className="text-sm font-semibold text-white">{team.teamName === 'Registered squads appear after confirmation' ? 'Registered players appear after confirmation' : team.teamName}{team.status ? <span className="ml-2 text-[10px] font-medium uppercase tracking-wide text-white/40">{team.status}</span> : null}</span><span className="text-right text-sm text-white/55">{team.matches}</span><strong className="text-right text-sm text-white">{team.points}</strong></div>)}</div></div></section>;
}
